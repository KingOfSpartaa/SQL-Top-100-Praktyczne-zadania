Dla każdego miasta klienta pokaż łączną liczbę zamówień oraz łączną wartość tych zamówień.

SELECT customers.City, COUNT(DISTINCT orders.OrderID) AS OrderCount, SUM(order_details.Quantity * products.Price) AS 'SUMA_ZAMOWIEN'
FROM customers
JOIN orders ON customers.CustomerID = orders.CustomerID
JOIN order_details ON orders.OrderID = order_details.OrderID
JOIN products ON order_details.ProductID = products.ProductID
GROUP BY customers.City 
