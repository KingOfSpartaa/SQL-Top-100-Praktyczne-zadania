Dla każdego pracownika wyświetl liczbę klientów, którym obsłużył zamówienia.

SELECT employees.EmployeeID, employees.LastName, COUNT(DISTINCT customers.CustomerID) AS UniqueCustomers
FROM orders
JOIN employees ON orders.EmployeeID = employees.EmployeeID
JOIN customers ON orders.CustomerID = customers.CustomerID
GROUP BY employees.EmployeeID, employees.LastName;
