Wyświetl listę zamówień z datą, imieniem klienta i łączną wartością zamówienia.


SELECT orders.OrderID, orders.OrderDate, customers.CustomerName, SUM(order_details.Quantity * products.Price) 
FROM orders
JOIN customers ON orders.CustomerID = customers.CustomerID
JOIN order_details ON orders.OrderID = order_details.OrderID
JOIN products ON order_details.ProductID = products.ProductID
GROUP BY orders.OrderID, orders.OrderDate, customers.CustomerName;
