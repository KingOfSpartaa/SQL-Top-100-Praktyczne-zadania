Dla każdego produktu pokaż, ilu unikalnych klientów go zamówiło.
SELECT products.ProductID, PRODUCTS.ProductName, COUNT(DISTINCT customers.CustomerID) AS 'UNIKALNY_KLIENT' FROM products
JOIN order_details ON products.ProductID = order_details.ProductID
JOIN orders ON order_details.OrderID = orders.OrderID
JOIN customers ON orders.CustomerID = customers.CustomerID
GROUP BY products.ProductID
ORDER BY COUNT(DISTINCT customers.CustomerID)DESC
