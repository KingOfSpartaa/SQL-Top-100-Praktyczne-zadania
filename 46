Wyświetl klientów, którzy nigdy nie złożyli żadnego zamówienia.

SELECT customers.CustomerName
FROM customers
LEFT JOIN orders ON customers.CustomerID = orders.CustomerID
WHERE orders.OrderID IS NULL;
