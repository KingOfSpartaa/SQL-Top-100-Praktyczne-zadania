Dla każdego klienta pokaż, ile dni minęło między jego pierwszym a ostatnim zamówieniem.

SELECT customers.CustomerName,
DATEDIFF(MAX(orders.OrderDate), MIN(orders.OrderDate)) AS DaysBetweenOrders
FROM customers
JOIN orders ON customers.CustomerID = orders.CustomerID
GROUP BY customers.CustomerName;
